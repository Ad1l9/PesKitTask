@model List<Tag>
@{
    int count = 1;
}
<div class="col-lg-12 stretch-card">
    <div class="card">
        <div class="card-body">
            <h2 class="card-title">Blog Table</h2>
            <div class="table-responsive pt-3">
                <a class="btn btn-info btn-primary" asp-controller="Tag" asp-action="Create">Add Category</a>
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>
                                No
                            </th>
                            <th>
                                Tag Name
                            </th>
                            <th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Tag tag in Model)
                        {
                            <tr class="table-info">
                                <td>
                                    @(count++)
                                </td>
                                <td>
                                    @tag.Name
                                </td>
                                <td>
                                    <a asp-action="Update" asp-route-id="@tag.Id" class="btn btn-warning btn-info"> <i class="mdi mdi-pencil"></i> Update</a>
                                    @if (User.IsInRole("Admin"))
                                    {
                                        <a asp-action="Delete" asp-route-id="@tag.Id" class="btn btn-danger btn-info"> <i class="mdi mdi-delete"></i> Delete</a>
                                    }
                                    <a asp-action="Detail" asp-route-id="@tag.Id" class="btn btn-primary btn-info"> <i class="mdi mdi-open-in-new"></i> Details</a>
                                </td>
                            </tr>
                        }

                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

